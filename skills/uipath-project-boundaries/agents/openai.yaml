interface:
  display_name: "UiPath Project Boundaries"
  short_description: "Define enforceable UiPath project boundaries"
  default_prompt: "Use $uipath-project-boundaries to define public entry points and dependency rules."
