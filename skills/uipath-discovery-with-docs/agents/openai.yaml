interface:
  display_name: "UiPath Discovery With Docs"
  short_description: "Run UiPath discovery with a durable paper trail"
  default_prompt: "Use $uipath-discovery-with-docs to discover this process and maintain its project records."
policy:
  allow_implicit_invocation: false
