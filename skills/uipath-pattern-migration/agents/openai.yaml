interface:
  display_name: "UiPath Pattern Migration"
  short_description: "Run bounded, reversible UiPath pattern migrations"
  default_prompt: "Use $uipath-pattern-migration to inventory, migrate, verify, and retire this repeated pattern."
