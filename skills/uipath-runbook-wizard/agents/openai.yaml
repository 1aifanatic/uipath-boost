interface:
  display_name: "UiPath Runbook Wizard"
  short_description: "Generate a safe wizard for human-only UiPath steps"
  default_prompt: "Use $uipath-runbook-wizard to turn these human-only UiPath steps into a verified staged wizard."
