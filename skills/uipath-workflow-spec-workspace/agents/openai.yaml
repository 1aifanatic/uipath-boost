interface:
  display_name: "UiPath Workflow Spec Workspace"
  short_description: "Develop recurring workflows into planner-ready specs"
  default_prompt: "Use $uipath-workflow-spec-workspace to maintain the durable specification workspace for this process."
policy:
  allow_implicit_invocation: false
