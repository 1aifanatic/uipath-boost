interface:
  display_name: "UiPath Task Refinement"
  short_description: "Refine UiPath plans into tracer-bullet tasks"
  default_prompt: "Use $uipath-task-refinement to turn this approved plan into session-sized tasks with blocking edges."
policy:
  allow_implicit_invocation: false
