interface:
  display_name: "UiPath Quality Gates Setup"
  short_description: "Install fast and comprehensive UiPath quality gates"
  default_prompt: "Use $uipath-quality-gates-setup to add proven local and CI checks to this UiPath repository."
policy:
  allow_implicit_invocation: false
