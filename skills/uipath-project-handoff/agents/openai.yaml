interface:
  display_name: "UiPath Project Handoff"
  short_description: "Create a portable UiPath project handoff"
  default_prompt: "Use $uipath-project-handoff to move verified context safely to another person, harness, or directory."
policy:
  allow_implicit_invocation: false
