interface:
  display_name: "UiPath Work Intake Triage"
  short_description: "Turn raw UiPath requests into verified work"
  default_prompt: "Use $uipath-work-intake-triage to classify, verify, and route this incoming work item."
policy:
  allow_implicit_invocation: false
